<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nojoo.com.rickandmortyencyclopedia">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="AllActivity"
            android:label="@string/app_name">
        </activity>
        <activity android:name="LocationActivity"
            android:label="@string/app_name">
        </activity>
        <activity android:name="StatusActivity"
            android:label="@string/app_name">
        </activity>
        <activity android:name="GenderActivity"
            android:label="@string/app_name">
        </activity>
        <activity android:name="SpeciesActivity"
            android:label="@string/app_name">
        </activity>
        <activity android:name=".MaleGender"
            android:label="@string/app_name">
        </activity>
        <activity android:name=".FemaleGender"
            android:label="@string/app_name">
        </activity>
        <activity android:name=".UnknownGender"
            android:label="@string/app_name">
        </activity>

    </application>

</manifest>